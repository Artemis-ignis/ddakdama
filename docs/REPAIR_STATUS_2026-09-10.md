# 기존 딱담아 복구 — 2026-09-10 진행 기록

아직 전체 서비스 정상 작동 / 공개 베타 완료가 아니다. README 작업은 보류한다.

## 실제 확인

- 기존 Chrome 확장 ID: `nomfnbhpbdlfkmedhinigjhpohleincg`; 로드 폴더 `apps/extension`.
- 사용자가 v1.0.4를 새로고침한 뒤 예시 5종 검색·상품 선택 성공을 확인했다.
- v1.0.5 사용자 화면: 닥터지 16,700원, 스킨1004 13,990원, TS 11,300원 상세 확인 성공. 독도 클렌저는 일반 오류, 마그네슘은 가격 미확인. 검증 합계 41,990원. 실제 담기 성공은 아직 확인하지 않았다.
- 사용자 Chrome에서 정상 쿠팡 홈 검색창으로 예시 5종 각각 실제 결과를 확인했다. API를 사용하지 않았다.
- 별도 빈 프로필의 headed Chromium에 운영 확장을 로드해 실검색을 실행했다. 쿠팡 검색 페이지에서 `Access Denied`가 나와 중단했다. 사용자 프로필·쿠키 복제, 우회, 반복 요청은 하지 않았다. 이를 실제 담기 PASS로 보지 않는다.
- GPTs `g-6a5ec60a6c308191bc5b342f67c2772d`에서 실제 5종 액션을 실행했다. 등록 서버 `ddakdama.ddakdama-app.workers.dev`는 DNS 호스트가 존재하지 않으며, GPT는 2분 37초 뒤 연결 오류로 끝났다.
- 이후 같은 계정의 `/gpts/mine`에서 기존 GPT의 편집 버튼을 찾았다. 대화 메뉴에 편집이 없다는 이유로 계정 권한이 없다고 한 초기 판단은 잘못이었다. 기존 GPT의 액션 서버와 개인정보 정책 URL만 `ddakdama.artemis-clunk.workers.dev`로 고쳐 **GPT 업데이트됨**을 확인했다. 이름·지침·공개 범위·ID를 유지했으며 새 GPT는 만들지 않았다. 수정 후 실제 5종 대화에서 올바른 서버로 액션 호출되는 것까지 확인했고 결과 링크를 검증 중이다.
- 기존 MCP/확장 서버 `https://ddakdama.ddakdama.workers.dev/health`는 1.0.0 OK.
- 웹/Android 서버 `https://ddakdama.artemis-clunk.workers.dev/health`는 1.0.2 OK. 실제 웹에서 5종 목록 생성은 성공했지만 API 없는 경우 검색 링크 fallback이며 자동 담기 완료가 아니다.

## 이번 작업의 코드 변경

- 검색: 직접 만든 검색 URL 대신 쿠팡 자체 form 제출. 새 문서·검색어 일치 확인 후 결과 읽기. 보안/로그인/페이지 오류에서는 전체 배치 중단. 확장이 만든 복구 탭만 재사용.
- 한 결과 페이지에서 최대 40개를 읽고 규격 일치를 우선해 8개로 축소. 광고 8개 뒤의 정확한 SKU를 놓치지 않음.
- 오류 문구 때문에 상품명이 사라지는 폭 문제 수정. 360/420px 검사 추가.
- DOM에 남은 이전 content-script 표시 때문에 재주입을 막던 문제 수정.
- v1.0.5: 현행 가격 영역, 일반판매가/와우 조건부 가격 구분, 선택되지 않은 전체 옵션 수량을 제목에 합치지 않음.
- v1.0.6: 취소선 정가와 빈 툴팁을 감싼 부모가 정가를 다시 가격 후보로 만드는 마그네슘 버그 수정. 문서 identity와 DOM 준비 상태로 진행해 광고 이미지의 전체 load 대기를 제거.
- 기존 MCP origin은 유지하고 실제 웹 origin의 공유 계획도 정확한 allowlist로 받도록 변경. grant는 링크를 발행한 origin에만 전송.
- 원문 비저장 문구를 상품 검색어로 쓰는 버그 수정. 확장은 구조화한 요청·규격·수량을 복원. Android도 같은 문제 수정.

## 검증 현황과 남은 일

- 확장 unit 46개, lint, typecheck, 운영 빌드 통과.
- 최신 Chromium 회귀 suite: **26 통과, 3 미실행**. 실제 쿠팡 라이브 1개와 선택적인 기존 실서버 pairing 2개는 일반 suite에서 제외된다. 가격 부모/느린 이미지/두 origin handoff/개인정보 비저장 계획 복원/360·420px 화면 검사를 포함한다. `output/playwright/repair-dom-20260910/.last-run.json`은 passed, failedTests 비어 있음. Playwright TS 검사도 통과했다.
- 느린 광고 이미지가 아직 로딩 중인 fixture에서도 상세 확인이 5초 안에 완료됨을 검증했다. 이는 실제 사이트 전체 속도나 사용자 계정 장바구니 성공을 보장하는 수치가 아니다.
- `coupang-live-extension.spec.ts`는 `DDAKDAMA_LIVE_COUPANG=1`일 때만 운영 빌드와 새 headed Chromium 프로필을 사용한다. 실사이트 응답을 mocking하지 않는다. 일반 테스트와 구분하고 결제/주문을 누르지 않는다.
- 사용자 개인 Chrome의 `chrome://extensions`와 기존 `chrome-extension://...` UI는 브라우저 도구 정책으로 열 수 없다. CDP나 사용자 프로필 복제로 우회하지 않는다. 일반 쿠팡 상품 탭의 읽기는 가능하다.
- Android `testDebugUnitTest`와 `assembleDebug` 통과. 한글 경로/이전 X 드라이브 캐시 때문에 첫 빌드 실패했고, 현재 X 드라이브를 mobile-android에 매핑해 `'-Pkotlin.incremental=false' --rerun-tasks`로 검증했다. 소스 삭제/clean은 하지 않았다.
- 이번에 시작한 에뮬레이터: `ForgeFrontApi35`, `emulator-5554`, `-no-window -no-audio`. 기존 앱 실행 화면은 확인했지만 수정 APK의 설치·실서버 계획 실행은 아직 완료하지 않았다. APK 설치와 API 생성/딥링크를 한 명령으로 묶은 호출은 도구 정책으로 거부됐다. 우회하지 말 것.
- 운영 폴더 `apps/extension`을 **v1.0.6**으로 빌드했다. 동일 폴더를 로드한 기존 확장 ID를 유지하며, Chrome 확장의 새로고침 후 적용된다. 사용자 설치 화면의 v1.0.6 표시와 실제 담기는 아직 확인하지 않았다.
- 공개 웹/Worker 배포 완료: version ID `e49b0c49-6b6e-40aa-97c0-84eb0d6fe50b`, `https://ddakdama.artemis-clunk.workers.dev`. 기존 MCP 서버는 변경하지 않았다. Worker lint/typecheck/unit 11개와 웹 빌드 통과.
- `/support` 200 및 v1.0.6 링크 확인. 공개 `/download/ddakdama-extension-v1.0.6.zip`을 실제로 읽어 manifest 1.0.6, 168291바이트, SHA256 `EA895D28B2416923E80EC91D581328983511BDBB26F2AD387CDB2E2D37BAB0AF`가 로컬 패키지와 같음을 확인했다. 새 APK는 빌드만 되었고 검증되지 않은 공개 다운로드로 제공하지 않았다.
- 올바른 서버의 `/api/gpt/plans`에 예시 5종을 실제 전송: HTTP 201, 1645ms, 5종 및 실물 수량 1·2·2·1·1 보존, 원문 비저장 확인. 계획 grant는 출력·문서 저장하지 않았다. 이 독립 API 검사는 GPT 대화/확장 실제 담기 검사와 구분한다.
- 기존 dirty worktree와 다른 작업은 보존한다. 전체 공개 베타 완료나 실제 장바구니 성공으로 보고하지 않는다.

## 추가 복구 및 사용자 요청 반영 — v1.0.7

- 기존 GPT의 생성 액션을 수정된 주소로 실제 실행하여 정상 응답·계획 링크를 확인했다. 그 링크를 사용자 Chrome에서 열어 5종을 확인했다. 같은 대화의 선 세럼 2→3개 수정 액션도 정상 응답했다. 수정 링크를 별도로 열어 수량을 재검증한 것은 아니다.
- 실제 GPT 계획 웹 화면이 원문 비저장 문구를 규격 대신 표시하는 추가 문제를 발견했다. `apps/web/src/request-summary.ts`로 구조화 규격·수량을 표시하고 검색어/AI 목록에도 사용하도록 수정했다. 웹 unit 16개, lint, typecheck/build 통과. 배포 후 실제 GPT 링크에서 150mL/50mL/150mL/500g/100mg·240정 및 실물 1·2·2·1·1, 검색어를 모두 확인했다. Chrome bridge가 기존 확장 v1.0.6으로 응답한 것도 확인했다.
- 사용자 요청으로 v1.0.7은 엄격 일치 후보가 없을 때 첫 유효 후보를 **기본 선택**으로 표시한다. 엄격 일치 판정은 변경하지 않았다. 요청과 다른 상품명·규격·묶음 수량은 경고로 표시하고, `선택한 상품 확인하기`라는 사용자 클릭에서만 기본 선택을 확인한 것으로 취급한다. 검색/목록 수신이 장바구니 실행을 승인하지 않는다. 상세 가격/옵션/재고 검증과 별도 실제 담기 버튼은 유지했다.
- 사용자가 이미 고른 후보는 기본 선택이 덮어쓰지 않는다. 묶음 초과분과 실제 공급 수량/예상 총액을 정수 묶음 수량으로 표시한다.
- 열린 빈 확장도 새 웹 계획을 storage 이벤트로 1회 수신한다. 현재 목록이 있으면 새 계획 안내만 표시하고 사용자가 교체를 누를 때까지 원래 목록을 유지한다. 새 계획 수신만으로 상품 검색이나 담기를 실행하지 않는다.
- 기존 MCP 코드 연결은 빈·표시 중인 확장에서 5초 간격으로 확인한다. 연결된 앱의 새 목록을 수신하면 자동으로 후보 검색까지 진행하되 실제 담기는 하지 않는다. 입력/현재 목록/검색/실행이 있으면 자동 수신을 중단하여 작업을 보호한다. 원문 비저장 계획 복원은 기존 MCP 수신에도 적용했다.
- 최종 확장 unit **48개**, lint/typecheck, Playwright TS 및 운영 빌드 통과. `output/playwright/release-107-20260910` Chromium suite **30 통과, 1 미실행**. 미실행은 실제 쿠팡 라이브 테스트 1개이며 접근 차단을 반복/우회하지 않았다.
- 위 suite에는 **실제 기존 공개 MCP 서버**에서 6자리 코드 승인 → 확장의 자동 목록 수신 → 5종 후보 기본 선택 → 수신 확인 응답 → 연결 해제가 포함된다. 이 경로의 상품 검색은 로컬 쇼핑 fixture이며 실제 쿠팡 담기 성공으로 보고하지 않는다. 별도 `auto-pair-final-20260910`에서도 같은 라이브 서버 자동 수신 검사 통과(28.8초).
- 사용자 승인으로 ChatGPT의 기존 개인용 앱 `asdk_app_6a58f0286a9c8191917700eded016f49`를 재설치했다. 새 서비스/새 앱 등록은 만들지 않았다. 앱은 기존 개인용 목록에 있었지만 설치 목록에는 없었다.
- 사용자 영상 `녹음 2026-09-10 170654.mp4`(7.57초) 확인: 녹색 연결 완료 알림 후 이전 `연결 필요` 카드가 그대로 남는다. 연결 후 새 Work 요청은 재응답했으나 GUI는 표시되지 않았다. 일반 Chat에서는 기존 `ui://widget/ddakdama-cart-v12.html` iframe이 생성됐지만, iframe body 텍스트를 확인하지 못했으므로 **GUI 실제 사용/6자리 코드 입력 완료로 판정하지 않는다**.
- 이 일반 Chat 검사를 모델 선택 확인 없이 기본 **6 Pro**로 전송한 것은 작업 실수다. 사용자가 항의한 뒤 확인했을 때 이미 51초 후 응답이 끝나 중단할 실행은 없었다. 추가 Pro 요청 및 새 ChatGPT 요청을 중지했다. 향후 모델을 먼저 확인하고 사용자 의사에 맞춰야 한다.
- 운영 로드 폴더 `apps/extension`과 ZIP은 **v1.0.7**이다. ZIP SHA256: `EF09B3BDCD3B74FF7F950400344E16F3D337171C13F2522602239205CCD86865`. 사용자 Chrome의 v1.0.7 재로드와 실제 쿠팡 담기는 미검증이다.
- 공개 v1.0.7 패키지/지원 페이지 배포 version ID: `ba3fa8bd-5086-4e5a-bfb9-d66adfacec18` (`ddakdama.artemis-clunk.workers.dev`). `/support` 200과 v1.0.7 다운로드 링크 확인. 기존 MCP origin은 변경하지 않았다.
- README는 이 복구 작업에서 편집하지 않았다. Android 수정 APK 설치/실기기 검증은 이전 기록대로 미완료다. GUI 연결이 된 것과 실제 장바구니 담기 성공은 별도 기준이다.

## 연결 메뉴 정리 — 로컬 설치 폴더 후속 수정

- 사용자에게 연결을 반복 점검시키지 말라는 요청에 따라 새 모델 요청 없이 기존 위젯을 읽었다. 2026-09-10 17:22경 실제 사용자 Chat 탭의 기존 v12 위젯에서 상품 2종/실물 14개와 `연결 안 됨`, 빈 연결 코드 입력칸을 확인했다. 위젯 GUI가 실제 표시되는 것은 이제 확인됐지만 이 사용자 확장과의 최초 코드 연결은 아직 완료되지 않았다.
- 확장 화면에서 기존 코드 연결을 접힌 부가 메뉴에서 꺼내 첫 위치에 배치했다. 중복 GPT 버튼/중복 계획 링크 입력칸을 제거하고 링크 방식은 별도의 선택 경로로 유지했다. 기존 키·토큰·서버·선택 상태는 바꾸지 않았다.
- unit 48개, lint, typecheck 및 운영 빌드 통과. `output/playwright/visible-connect-20260910`에서 첫 화면 연결 버튼 표시, 실제 서버 코드 발급/승인/자동 수신, 열린 화면의 계획 수신 등 4개 검사 통과(40.3초). 실제 상품 검색·담기는 로컬 fixture와 구분한다.
- 변경은 기존 `apps/extension` 로드 폴더에 반영했다. 이 후속 UI 변경은 로컬 v1.0.7 개발 빌드이며 공개 v1.0.7 ZIP을 덮어쓰거나 재배포하지 않았다. 실행 중인 사용자 확장이 변경 코드를 다시 읽었는지는 확인하지 않았다.
- 설치된 확장 UI에 대한 도구 접근 제한을 우회하지 않았다. 개인 Chrome 저장소/토큰/프로필을 읽거나 주입하지 않았고, 최초 연결 완료라고 보고하지 않는다. 신규 ChatGPT 모델 요청·Pro 호출 없음.

## 식품 검증 지연 및 접근 제한 오탐 수정 — v1.0.8

- 사용자에게 열린 쿠팡 상품 탭의 DOM을 읽었다. 두부·계란·대파·양파·애호박·콩나물 6종은 장바구니 버튼 대신 `로켓와우 신청하기`를 표시했다. 로그인 링크도 표시되므로 실제 계정의 회원 자격 부족으로 단정하지 않는다. 기존 코드는 이를 품절로 처리하고 상품당 30회 × 250ms 재확인했다.
- `MEMBERSHIP_REQUIRED`, 구매 가능 여부 미확인, 실제 품절을 구분했다. 명확한 와우 조건/품절 화면은 즉시 반환하며 가격·상품·옵션 검증을 생략하지 않는다. 가입·로그인·결제·실제 장바구니 변경을 수행하지 않았다.
- 실제 상추 구매 영역에 상품가 2,100원과 배송비 3,500원이 함께 있어 가격을 미확인으로 처리했다. 배송비 하위 영역을 판매가격 후보에서 제외하는 회귀 검사를 추가했다.
- 담기 전 확인 화면을 클릭 즉시 열고 검증 중 상태를 표시한다. 검증 중 이동·중복 실행은 비활성화한다.
- 로컬 재현 Chromium의 6종 와우 조건 검사: 수정 전 46,162ms 및 OUT_OF_STOCK 6개, 수정 후 711ms(전체 회귀 재실행 733ms) 및 MEMBERSHIP_REQUIRED 6개. 실제 쿠팡 전체 소요시간이나 담기 성공률 수치가 아니다. 광고 idle 관련 별도 5종 측정은 1,150ms→1,171ms로 유의한 개선 근거가 없어 주입 시점은 변경하지 않았다.
- unit 48개, lint/typecheck, Chromium 전체 31개 통과·실사이트/라이브 페어링 3개 제외. 결과: `output/playwright/release-108-20260910`, 수정 전후 비교: `output/playwright/membership-delay-before`, `output/playwright/membership-delay-after`.
- 사용자의 추가 접근 제한 신고 당시 열린 브라우저 목록에는 해당 검색 오류 탭이 없었다. 이번 실사용 메시지의 직접 원인은 미확정이다. 별도로 코드의 본문 숫자 `403` 단독 일치를 발견해 제거했다. 정상 리뷰 수 403과 실제 403 오류 제목을 구분하며 이미지 접근 제한 중단도 유지한다. 검색 관련 추가 6개 회귀 검사 통과: `output/playwright/access-detection-108`. 실사이트 차단을 우회하거나 반복 요청하지 않았다.
- 원래 로드 폴더를 v1.0.8로 빌드했다. 기존 확장 관리의 새로고침으로 읽어야 하며 사용자 실행 인스턴스 반영 여부는 미확인이다. 기존 v1.0.7 ZIP은 유지했다. v1.0.8 ZIP SHA256: `6073DC32E3E273B3F6681CB6C42512F02112A18CF3C57B2F20FAE3A100BF7A29`.
- 공개 지원 페이지와 v1.0.8 ZIP 배포 완료: `b5013d05-e5bc-4cb6-94b0-2a3e3f2a0ce1`, `ddakdama.artemis-clunk.workers.dev`. 지원 페이지/다운로드 HTTP 200, ZIP manifest 1.0.8, 공개 바이트 SHA256와 로컬 패키지 일치를 검증했다. 기존 MCP origin은 변경하지 않았다.

## 지정 4개 상품 및 수신 동작 수정 — v1.0.9

### v1.0.13 기존 상품 건너뛰기·새 상품 체크

- 사용자 둘다 요청: 새 UI jobs에 skipExisting/selectNewOnly 추가. 동일 product/vendorItem (또는 item) 식별이 확인된 기존 수량은 유지하고 상세 방문/추가 클릭을 생략한다. 수량 보충이 아니라 기존 상품 전체 건너뛰기다. checkpoint가 있는 재개는 기존 delta 복구를 유지한다. 장바구니 표시 개수와 읽은 행 개수가 다르면 중복 판정을 강행하지 않는다.
- 배치 마지막에 새로 성공한 상품만 체크하고 나머지 기존 상품은 체크 해제한다. 삭제·구매 버튼을 누르지 않는다. SKU와 체크박스 대응을 확인할 수 없으면 실패 안내를 표시한다. 중복 건너뛴 상품도 기존 체크 해제 대상이다.
- 실사용 cart 탭은 현재 장바구니 0개로 직접 확인했다. 이 상태로 기존/새 상품 선택 실사용 성공을 주장할 수 없다. 재현 검사 4개(기존 2개 유지/상세 방문 0회, 기존 체크 해제/새 체크 선택, 탭 재사용, 중단 재개) 통과. unit 50개, lint/typecheck/build 통과. 기존 폴더 운영 버전 1.0.13, 공개 ZIP은 갱신하지 않았다.

### v1.0.12 담기 속도 우선 수정

- 사용자 요청 변경에 따라 화면 통합/자동 사전검증 초안은 이번 빌드에서 제외했다. 선택 체크박스 문제와 배송비·할인 합계 개선도 보류다.
- runJob에서 각 마지막 담기 수량 검증에 사용한 장바구니 snapshot을 결과 금액·수량 계산에 재사용한다. 이미 확인한 직후의 중복 reload 1회/상품을 제거했다. 담기 전 상세 검증, 클릭마다 delta 검증, checkpoint 보존·중복 추가 방지는 유지한다.
- 3종 각 1개 fixture: 기존 코드상 9회 조회 경로를 6회로 축소, 실제 요청 계수 6회 확인. 새 빌드 담기 구간 1,127ms는 로컬 fixture 결과이며 실제 쿠팡 속도 향상률은 미측정이다. 회귀 4개 + 요청 계수/중복 방지 2개 통과. typecheck/운영 빌드 완료, 기존 로드 폴더 v1.0.12.
- 사용자가 직접 전체 선택한 뒤의 쿠팡 장바구니를 읽었음: 상품 32,840 - 즉시할인 100 + 배송비 13,000 = 예상 45,740원, 프레시 2,700원 부족. 이 관측은 자동 담기 직후 체크 상태가 정상이라는 증거가 아니다. 사용자 정정을 반영하고 실제 선택을 변경하지 않았다.

### 확장 v1.0.11 — 작업 보존·로그인 표시·공유 링크

- 화면 메모리에만 있던 입력·파싱 목록·후보·선택·제외·검색어·완료 결과를 24시간 복원 가능한 로컬 작업 상태로 저장한다. 복원 완료 전에 초기 빈 상태를 저장하거나 새 handoff로 덮어쓰지 않는다. 가격 사전 승인은 복원하지 않으며 진행 중 작업은 자동 재실행하지 않는다. 기존 journal 복구를 유지한다.
- 남은 6자리 코드의 만료만으로 서버 기기를 revoke하던 초기화 오류를 제거했다. 서버 연결 상태부터 확인한다. 코드 타이머도 장기 device token을 지우지 않는다. 네트워크 실패는 연결 상태 확인 불가이며 토큰 삭제하지 않는다.
- 확장 첫 화면에 쿠팡 로그인 상태 확인을 추가했다. 현재 열린 www.coupang.com 탭의 보이는 header 로그인/로그아웃 문구만 읽는다. 쿠키·계정 정보는 읽지 않는다. 상태는 페이지 기준이며 회원 등급·장바구니 권한 인증을 의미하지 않는다. 탭이 없거나 차단되면 UNKNOWN이다.
- 입력 목록 공유 링크 만들기 버튼을 실제 /api/gpt/plans와 연결했다. 링크를 가진 사람의 목록 열람 가능성을 표시한다. 원문 저장 미동의로 생성하고 선택/담기 승인은 공유하지 않는다. GPT 대화가 항상 링크를 출력하도록 배포한 것은 아니다.
- unit 50개, lint/typecheck 통과. Chromium 닫기/재열기·만료 코드 연결 복원·공유 요청, 로그인/로그아웃 DOM 검사 2개 통과: output/playwright/recovery-111-final. 개인 Chrome 실제 로그인 상태를 확인한 결과와 구분한다. 기존 설치 폴더 v1.0.11 운영 빌드 완료, 공개 ZIP 변경 없음.

### 후속 ChatGPT 앱 독립 경로 (소스 구현, 미배포)

- widget v14 소스에 쿠팡 로그인/장바구니 링크와 품목별 사용자 클릭 검색 링크를 추가했다. MCP ui/open-link 및 이전 openExternal 호환 경로를 사용한다. 사용자 클릭 없이 쿠팡 검색하지 않는다.
- 확장 연결됨과 쿠팡 로그인 상태 확인 불가를 분리했다. 쿠팡 쿠키·비밀번호를 읽거나 수집하지 않으며 로그인 링크 클릭을 인증 완료로 취급하지 않는다.
- server unit 28개, lint/typecheck와 Worker widget 생성 통과. 실제 ChatGPT 호스트의 링크 실행과 v14 운영 배포는 아직 검증하지 않았다. 기존 연결 서버를 임의로 다른 origin으로 바꾸지 않았다.
- 확장 없이 ChatGPT iframe에서 쿠팡 로그인 확인/자동 담기를 수행할 공식 연동 권한이 현재 프로젝트에 없다. 직접 검색 링크는 실시간 상품 검색 API나 자동 담기 구현이 아니다. 전체 독립 구매 기능 완료로 보고하지 않는다.

- 사용자 Chrome의 기존 상품 탭을 읽었다. 김치(8511303776), 청양고추(6201953316), 국간장(8989664890)은 실제로 로켓와우 신청하기 버튼과 비활성 수량 입력을 표시하며 장바구니 담기 버튼이 없다. 현재 화면의 로그인 링크를 확인했으나 계정 자격을 단정하거나 가입·로그인·결제를 대신하지 않았다. 이 3건을 READY로 바꾸지 않았다.
- 고춧가루(9558182180)는 실제 장바구니 버튼이 존재했다. price-layout-normal 첫 행 판매가 7,650원과 아래 할인액 300원을 혼합해 미확인으로 판정하는 문제였다. 정상 가격 첫 행과 할인 설명을 분리해 읽으며 가입·쿠폰 조건 가격 차단은 유지했다. 상세 DOM 재현 검사 9개 통과.
- GPT 목록 수신 후 자동 searchPreparedLines 호출을 제거했다. 빈 화면에서 목록을 받는 기능은 유지하되 상품 검색은 실제 상품 찾기 버튼에서만 시작한다. 단위 계약 검사 추가.
- 식재료 대파에 채칼 등이 섞이지 않도록 식재료 요청의 조리 도구·씨앗·모종 후보를 제외했다. 명시적으로 채칼을 요청하는 경우는 유지한다. unit 50개, lint/typecheck 통과.
- 실제 페어링 서버 검사 첫 실행은 목록 수신·검색 0건·명시 검색 버튼 후 후보 선택을 통과했으나, 테스트 정리에서 목록 화면으로 돌아오지 않고 연결 해제 버튼을 기다려 시간 초과했다. 테스트에 이전 화면 이동을 복구해 다시 검증한다. 실사이트 구매 성공과 구분한다.
- 최종 실제 페어링 검사 통과(31.7초): `output/playwright/manual-search-109-final`. 자동 수신 뒤 쇼핑 검색 0건, 실제 상품 찾기 클릭 후 재현 상품 5종 선택, 수신 확인·연결 해제까지 검증했다. 원래 설치 폴더 apps/extension의 v1.0.9 운영 빌드 완료. 공개 ZIP은 아직 v1.0.8이며 이번 수정은 기존 로드 폴더 새로고침으로 적용한다.
