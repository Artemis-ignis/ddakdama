# 문제 해결

## 매니페스트를 읽을 수 없음

`C:\Users\50106\Desktop\딱담아\apps\extension`을 선택했는지 확인하고 `pnpm build`를 실행합니다.

## 사이드 패널이 흰 화면

Chrome 확장 프로그램 카드에서 **업데이트**를 누른 뒤 기존 패널을 닫고 다시 엽니다. `apps\extension\dist\index.html`이 `./assets/` 상대경로를 사용하는지 확인합니다.

## GPT 앱 연결 실패

`start-windows.bat` 창이 열려 있는지, `http://localhost:8787/health`가 응답하는지 확인합니다. 새 6자리 코드는 10분 안에 한 번만 사용합니다.

## 가격 확인 실패

가격을 0원으로 처리하거나 자동 담기하지 않습니다. 상품 페이지를 열어 품절·필수 옵션·보안 확인 여부를 확인한 뒤 다시 검색합니다.

## 일부 상품만 담김

전체 성공으로 표시하지 않습니다. 실패 상품 상태와 담기 전후 수량을 확인하고 해당 상품만 다시 시도합니다.
