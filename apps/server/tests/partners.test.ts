import{describe,expect,it}from"vitest";import{authorization,normalizeDeepLinkPayload,normalizeSearchPayload,signedDate}from"../src/partners.js";
describe("쿠팡 파트너스 HMAC",()=>{it("UTC 서명 시각을 만든다",()=>expect(signedDate(new Date("2026-07-13T01:02:03Z"))).toBe("260713T010203Z"));it("동일 입력에 안정적인 CEA 헤더를 만든다",()=>{const value=authorization("GET","/v2/providers/affiliate_open_api/apis/openapi/v1/products/search?keyword=test",{accessKey:"access",secretKey:"secret",subId:"ddakdama"},new Date("2026-07-13T01:02:03Z"));expect(value).toContain("CEA algorithm=HmacSHA256");expect(value).toContain("access-key=access");expect(value).toContain("signed-date=260713T010203Z");expect(value).toMatch(/signature=[a-f0-9]{64}$/)});it("검색 응답을 확장 후보로 정규화한다",()=>{const results=normalizeSearchPayload({data:{productData:[{productId:123,productName:"선 세럼 50ml, 2개",productPrice:21800,productUrl:"https://www.coupang.com/vp/products/123?itemId=11&vendorItemId=22",isRocket:true},{productId:124,productName:"선 세럼 50ml x2 (증정 10ml)",productPrice:22800,productUrl:"https://www.coupang.com/vp/products/124?itemId=12&vendorItemId=23",isRocket:true}]}});expect(results[0]).toMatchObject({productId:"123",vendorItemId:"22",itemId:"11",currentPrice:21800,unitsPerPackage:2,source:"PARTNERS"});expect(results[1]).toMatchObject({productId:"124",unitsPerPackage:2})});it("딥링크 응답을 원본 URL과 매핑한다",()=>expect(normalizeDeepLinkPayload({data:[{originalUrl:"https://www.coupang.com/a",shortenUrl:"https://link.coupang.com/b"}]})[0].landingUrl).toBe("https://link.coupang.com/b"))});
